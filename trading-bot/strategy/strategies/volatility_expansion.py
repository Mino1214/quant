"""Stub: volatility expansion strategy."""
from typing import Optional
from strategy.base import StrategyContext
from core.models import Signal


def evaluate(context: StrategyContext) -> Optional[Signal]:
    return None
